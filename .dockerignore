# Deny everything, then admit what the build needs. A deny-list would keep
# admitting whatever is added next: `COPY . .` was pulling in a 315 MB .venv
# and the .git history.
*

!pyproject.toml
!README.md
!LICENSE
!src/
